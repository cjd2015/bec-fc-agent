# 域名 / SSL / 安全组接入清单

**版本:** 1.0  
**更新日期:** 2026-03-31

## 1. 域名接入清单
- [ ] `datahive.top` 已解析到 ECS 公网 IP
- [ ] `www.datahive.top` 是否需要同步解析
- [ ] DNS 生效已验证
- [ ] 80 端口访问可跳转 443

## 2. SSL 接入清单
- [ ] SSL 证书文件已上传到 ECS
- [ ] Nginx 已配置 `ssl_certificate`
- [ ] Nginx 已配置 `ssl_certificate_key`
- [ ] TLS 版本限制为 TLS1.2 / TLS1.3
- [ ] HTTPS 可正常访问
- [ ] 证书有效期已确认
- [ ] 证书续期方案已确认

## 3. FC 接入清单
- [ ] FC 服务已部署成功
- [ ] FC 公网地址可访问
- [ ] `/api/v1/health` 返回正常
- [ ] Nginx `/api/` 反向代理已配置
- [ ] 前端通过 `/api/` 成功访问 FC

## 4. ECS 安全组清单
### 必须开放
- [ ] 80（HTTP）
- [ ] 443（HTTPS）
- [ ] 22（SSH，仅限可信 IP）

### 数据库端口
- [ ] 5432 / 3306 不对全网开放
- [ ] 仅允许可信来源 IP
- [ ] 已配置白名单

### 原则
- [ ] 禁止数据库端口对 `0.0.0.0/0` 开放
- [ ] 只开放必要端口
- [ ] 尽量缩小公网暴露面
