# 踩坑记录

## 2026-04-06 - 更新前端前忘记关闭 8080 端口

- **现象**：我在本地调试时保留了 8080 端口的旧服务，导致你访问时仍命中旧版本页面（只显示 20 条词汇/句型）。
- **影响**：你无法验证最新构建效果，以为数据没有扩容。
- **规避措施**：今后每次准备替换前端静态资源或发布前，先确认并关闭 8080 端口上残留的旧服务（`lsof -i :8080` / `kill`），确保外部访问只有新版本。
